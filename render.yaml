services:
  - type: web
    name: ai-company-os
    runtime: docker
    plan: starter
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    preDeployCommand: alembic upgrade head
    healthCheckPath: /ready
    autoDeployTrigger: checksPass
    maxShutdownDelaySeconds: 60
    envVars:
      - key: APP_ENV
        value: production
      - key: AUTO_CREATE_SCHEMA
        value: "false"
      - key: TASK_EXECUTION_MODE
        value: worker
      - key: AUTH_ENABLED
        value: "true"
      - key: APP_API_KEY
        generateValue: true
      - key: DEFAULT_TENANT_ID
        value: owner
      - key: AI_PROVIDER
        value: openai
      - key: OPENAI_MODEL
        value: gpt-5.6-luna
      - key: OPENAI_API_KEY
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_ALLOWED_CHAT_ID
        sync: false
      - key: TELEGRAM_WEBHOOK_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: ai-company-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: ai-company-queue
          property: connectionString

  - type: worker
    name: ai-company-worker
    runtime: docker
    plan: starter
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: celery -A app.worker.celery_app worker --loglevel=INFO
    autoDeployTrigger: checksPass
    maxShutdownDelaySeconds: 120
    envVars:
      - key: APP_ENV
        value: production
      - key: AUTO_CREATE_SCHEMA
        value: "false"
      - key: TASK_EXECUTION_MODE
        value: worker
      - key: AUTH_ENABLED
        value: "true"
      - key: AI_PROVIDER
        value: openai
      - key: APP_API_KEY
        fromService:
          type: web
          name: ai-company-os
          envVarKey: APP_API_KEY
      - key: OPENAI_MODEL
        value: gpt-5.6-luna
      - key: OPENAI_API_KEY
        fromService:
          type: web
          name: ai-company-os
          envVarKey: OPENAI_API_KEY
      - key: TELEGRAM_BOT_TOKEN
        fromService:
          type: web
          name: ai-company-os
          envVarKey: TELEGRAM_BOT_TOKEN
      - key: TELEGRAM_ALLOWED_CHAT_ID
        fromService:
          type: web
          name: ai-company-os
          envVarKey: TELEGRAM_ALLOWED_CHAT_ID
      - key: TELEGRAM_WEBHOOK_SECRET
        fromService:
          type: web
          name: ai-company-os
          envVarKey: TELEGRAM_WEBHOOK_SECRET
      - key: DATABASE_URL
        fromDatabase:
          name: ai-company-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: ai-company-queue
          property: connectionString

  - type: keyvalue
    name: ai-company-queue
    plan: starter
    region: singapore
    ipAllowList: []
    maxmemoryPolicy: noeviction

databases:
  - name: ai-company-db
    plan: basic-256mb
    region: singapore
    databaseName: ai_company
    user: ai_company
    ipAllowList: []
